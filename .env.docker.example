# Copie para .env na raiz e ajuste antes de: docker compose up --build
# cp .env.docker.example .env

SECRET_KEY=troque-por-uma-chave-longa-e-aleatoria

# URL pública do app (onde o usuário abre no navegador)
FRONTEND_URL=http://localhost:8080
CORS_ORIGINS=http://localhost:8080,http://localhost:5173

# Admin criado automaticamente na 1ª subida
ADMIN_CPF=05721845732
ADMIN_SENHA=260281xx
ADMIN_NOME=Administrador
ADMIN_EMAIL=admin@parking.local

# Email (opcional — reset de senha)
# MAIL_USERNAME=
# MAIL_PASSWORD=
# MAIL_FROM=noreply@example.com
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
